const Town = require(`../models/town.model`);

const Discord = require(`discord.js`)
const { config, client } = require(`../index.js`);

client.on(`guildMemberAdd`, member => {
    // Put stupid welcome command here
});
